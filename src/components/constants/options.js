const options = [
    "Study Resource",
    "Productivity",
    "Time Management",
    "Effective Learning",
    "Short"
  ];

export default options.map(option => ({ name: option, selected: false}));